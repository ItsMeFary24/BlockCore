export const CustomCommandConfig = {
    set_home_max: {
        // This is the default limit of player home (no tag)
        default: 5,
        // When user have this tag, user can create 12 home
        "perms.sethome.12": 12,
    },
};
